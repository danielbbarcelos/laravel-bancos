# Copie para sicredi-sandbox.env e preencha com suas credenciais de SANDBOX.
# Este arquivo (sicredi-sandbox.env) é gitignored — nunca será commitado.

export SICREDI_BASE="https://api-parceiro.sicredi.com.br/sb"
export API_KEY=""        # x-api-key (Client ID do Portal)
export USERNAME=""       # beneficiário + cooperativa (9 dígitos), ex: 123450101
export PASSWORD=""       # código de acesso (Internet Banking)
export COOPERATIVA=""    # 4 dígitos
export POSTO=""          # 2 dígitos
export COD_BENEF=""      # código do beneficiário (5 dígitos)
